<%= form_for(@tweet_collection) do |f| %>
  <% if @tweet_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tweet_collection.errors.count, "error") %> prohibited this tweet_collection from being saved:</h2>

      <ul>
      <% @tweet_collection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
